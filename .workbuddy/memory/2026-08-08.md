# 2026-08-08

- 创建 `feature/gamified-learning` 分支（基于 main），用于游戏化学习功能开发。
- 撰写 `docs/specs/gamified-learning-spec.md` 与 `gamified-learning-plan.md`（挑战岛/Kid Quest，面向 7-15 岁，双内容来源：个人 KB + 新增 public KB 类型；新增 `kid_quest` Capability 与 `services/gamification` 引擎）。
- 注意：`/docs/specs/*` 在 .gitignore 中被有意忽略（现有 spec 也未跟踪），文档不提交 git。
- 决策（用户确认）：账号体系先行——采用"监护人主账号 + 儿童 profile"（无独立儿童登录，PIN 切换，角色能力矩阵守卫）。spec 新增 §2.3/§2.4 与 profile API，开放问题 #2 关闭；plan 新增 M0（账号与角色地基，5 天），M1 可与 M0 并行，总排期约 28 个工作日。
- 架构师高见远重写 plan 为完整版（1257 行）：Part A 技术架构（文件列表/类图/时序图/依赖/共享知识/OQ）+ Part B 任务分解（B0 追溯矩阵 F0-F11、T01-T05 五大任务、Mermaid 依赖图+甘特图、14 条风险登记册、5 层测试策略、5 MR 发布计划）。关键路径 T01→T03→T04→T05 约 30 天，总 37 工作日。
- 完成 docs/specs/ 下全部 plan：新增 `byok-multi-user-plan.md`（BYOK 多用户，13 任务 T01-T13，发现 vault/policy/grants/execution_source/byok_usage/API 大量已实现，重点为加固补全）和 `commercial-foundation-plan.md`（商业版基础，5 任务 T01-T05，发现 deeptutor/commercial/ 下 20+ 文件 7 表完整骨架已存在，重点为端到端集成验证/安全审计/品牌/部署验收）。两份均含完整追溯矩阵、Mermaid 图、风险登记册。

# 2026-08-09

- 游戏化学习功能全部 5 个任务（T01-T05）实现完成，提交到 `feature/gamified-learning` 分支（commit c04e2f90，78 文件，15026 行）。
- 410 测试全绿（引擎 55 + 徽章 18 + 存储 16 + profiles 24 + 角色守卫 40 + sources 25 + public_themes 20 + forging 19 + grading 32 + hints 16 + questing 14 + safety 52(含红队) + quest_cache 16 + kids API 18 + 聚合 20 + metrics 10 + models 扩展等）。
- 后端：gamification 引擎（纯函数零 LLM）、badges、store（atomic_write_json）、kid_profiles（models/store/pin bcrypt/guard 角色矩阵/kid_context ContextVar）、SourceResolver（命名空间隔离）、public_themes（3 主题 YAML）、safety filter（中英黑名单+红队≥98%）、quest capability（sourcing→forging→questing 三阶段、8 题型、确定性判分、三段式提示、缓存）、metrics（JSONL）、家长面板聚合函数。
- 前端：5 页面（home/map/play/rewards/layout）+ 10 组件（MapNode/QuestionCard/FeedbackOverlay/HintPanel/LevelSummary/BadgeGrid/StreakCalendar/ParentDashboard/RestReminder + ProfileSwitcher 内嵌）+ 3 hooks（useKidQuest/useKidProfile/useTTS）+ store + theme + API 封装 + 类型定义 + zh/en i18n。用 React Context+useReducer 替代 Zustand（项目无此依赖），零新增 npm 依赖。
- 公共库数据（data/public_kb/）：3 主题各含 manifest.yaml + 5 篇中文语料（恐龙世界 7-9 / 太阳系 10-12 / 乘法王国 7-9），被 .gitignore 排除但本地存在。
- 后端零新增依赖（刻意标准库），前端零新增依赖。
