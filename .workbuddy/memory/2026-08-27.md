# 2026-08-27

## Deployed gamified-learning (v1.6.0) to VPS "usa"

- VPS host `usa` = 100.126.160.20 (user hubo), deploy dir `/home/hubo/deeptutor`.
- Deployed prebuilt image `ghcr.io/hubo1989/deeptutor:latest` (=`1.6.0`, merged PR #8, rev `5def609`).
- Why not the existing compose files: VPS `docker-compose.ghcr.yml` was an OLD partial-mount/`8001`-port version, and base `docker-compose.yml` would build locally from stale PR #5 code. So created `/home/hubo/deeptutor/docker-compose.deploy.yml` that mirrors the RUNNING container exactly (network `deeptutor_deeptutor`, ports `127.0.0.1:8002->8001` + `127.0.0.1:3782->3782`, `./data:/app/data` volume, same SMTP env + healthcheck) and only swaps the image to `hubo1989:latest`.
- Auth fix: VPS `~/.docker/config.json` ghcr token was expired (pull denied). GitHub REST API CANNOT flip container-package visibility (only the web UI can) — `write:packages` token still 404s on PATCH. Solved by `docker login ghcr.io` on VPS with a valid `read:packages` token (`gh auth token`). Package stays PRIVATE.
- Data preserved: `./data` bind mount (accounts/system/users). Also made safety backup at `.deploy-backups/pre-deploy-20260827T033706Z`.
- Verified: container healthy, backend `:8002` HTTP 200, frontend `:3782` HTTP 307 (normal Next.js redirect), `quest` capability present, version 1.6.0. `deeptutor-pocketbase` left running.
- Future redeploy: `cd /home/hubo/deeptutor && docker compose -f docker-compose.deploy.yml pull && docker compose -f docker-compose.deploy.yml up -d`
